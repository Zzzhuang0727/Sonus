{
  "name": "sonus",
  "version": "0.1.0",
  "private": true,
  "description": "Sonus is a local-first AI DJ and personal radio host.",
  "type": "module",
  "packageManager": "pnpm@11.2.2",
  "scripts": {
    "dev": "corepack pnpm -r --parallel --if-present dev",
    "build": "corepack pnpm -r build",
    "test": "corepack pnpm -r test",
    "typecheck": "corepack pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
